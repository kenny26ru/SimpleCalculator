import java.util.ArrayList;
import java.util.List;

public class Arabs {
    static List<Integer> map = new ArrayList<>();
    static {
        for (int i = 0; i <=10 ; i++)
        {
            map.add(i);
        }
    }
}
